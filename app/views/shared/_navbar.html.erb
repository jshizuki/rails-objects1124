<nav class="navbar sticky-top navbar-expand p-1" style="background-color: white";>
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item"><%= link_to "All invoices", objects_invoices_path, class: "fs-3 nav-link" %></li>
        <li class="nav-item"><%= link_to "New invoice", new_objects_invoice_path, class: "fs-3 nav-link" %></li>
      </ul>
      <% if user_signed_in? %>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: "Log out?"}, class: "fs-3 nav-link" %></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
